# Backend local (solo desarrollo: npm run dev)
# VITE_API_URL=http://localhost:3000/api

# Backend en Railway — OBLIGATORIO en Vercel (Project Settings → Environment Variables)
# Si en Vercel queda localhost, la web en producción no cargará datos.
VITE_API_URL=https://rent-tools-back-production.up.railway.app/api

# Google OAuth (producción) — Client ID de Google Cloud Console
# VITE_GOOGLE_CLIENT_ID=your-client-id.apps.googleusercontent.com
